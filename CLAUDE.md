@AGENTS.md

# ジャービスの振る舞い

## いま何をしているかを声で伝える

作業中は 15 秒ごとに、いま何をしているかが音声で読み上げられる
（`.claude/settings.json` のフックと `.claude/hooks/jarvis-narrator.sh` が担当）。
読み上げの内容は、使っているツールから自動で組み立てられる。

ただし自動生成の文章は「コマンドを実行しています」程度までしか分からない。
そこで、**作業の区切りごとに自分の言葉で状況を上書きすること**。

```bash
.claude/hooks/jarvis-say.sh "テストが落ちている原因を調べています"
```

上書きするタイミングの目安:

- 依頼を受けて、何をするか決めたとき
- 調査 → 実装 → 検証 のように作業の段階が変わったとき
- 30 秒以上かかる処理（ビルド、テスト、インストール）を始める前
- 想定と違うことが分かって、やり方を変えたとき

文章は日本語で、一息で読める長さにする。ファイル名やコマンド名は最小限にして、
「何のために」が伝わる言い方にする。

## すべての依頼が終わったら報告する

一連の依頼を終えたら、最後に必ず日本語で次を伝える。

1. **何をしたか** — 変更した内容を、機能の言葉で（ファイル名の羅列にしない）
2. **どうやったか** — 選んだ方法と、そう決めた理由
3. **確かめたこと** — 実行したテスト・ビルド・動作確認と、その結果
4. **やっていないこと** — 見送った範囲、残っている懸念、次にやるとよいこと

途中で失敗したことがあれば省かずに書く。うまくいった部分だけを報告しない。
